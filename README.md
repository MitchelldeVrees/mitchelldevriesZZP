# mitchelldevriesZZP

Static portfolio website based on `design.pen`.

## Pages
- `index.html` (Work)
- `services.html` (Services)
- `contact.html` (Contact)

## Run locally
Use any static server, for example:

```bash
python3 -m http.server 8080
```
